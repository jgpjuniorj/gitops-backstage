
apiVersion: v1
kind: ConfigMap
metadata:
  name: backstage-app-config
  namespace: backstage
data:
  app-config.yaml: |
    app:
      title: Backstage Demo
    backend:
      baseUrl: http://backstage.127.0.0.1.nip.io
      listen:
        port: 7007
    techdocs:
      builder: 'local'
      generator:
        runIn: 'local'
